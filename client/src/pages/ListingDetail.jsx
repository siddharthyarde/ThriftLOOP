// Placeholder — implemented in DOC 4 (Listings module).
const ListingDetail = () => (
  <div className="max-w-2xl mx-auto px-4 py-16 text-center text-gray-400">
    Listing detail page — coming with the Listings module.
  </div>
);

export default ListingDetail;
